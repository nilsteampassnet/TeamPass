# Includes directory — disable directory listing.
# Only logout.php and language/*.txt files should be accessible via HTTP.
Options -Indexes

# Block direct PHP execution except logout.php
<FilesMatch "\.php$">
    Require all denied
</FilesMatch>
<FilesMatch "^logout\.php$">
    Require all granted
</FilesMatch>
