# Static assets — disable directory listing and PHP execution.
# Only static files (CSS, JS, images, fonts) should be served from here.
Options -Indexes
AddHandler cgi-script .php .php3 .php4 .phtml .pl .py .jsp .asp .htm .shtml .sh .cgi
Options -ExecCGI
