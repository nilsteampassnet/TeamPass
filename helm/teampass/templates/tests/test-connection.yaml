apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "teampass.fullname" . }}-test-connection"
  labels:
    {{- include "teampass.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "teampass.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
