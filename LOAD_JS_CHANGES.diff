diff --git a/includes/core/load.js.php b/includes/core/load.js.php
index 9247a2c0..81df3412 100755
--- a/includes/core/load.js.php
+++ b/includes/core/load.js.php
@@ -502,83 +502,6 @@ if (
     // Countdown
     countdown();
 
-    // Automatic session refresh on user activity
-    // This extends session automatically when user is actively using the application
-    (function() {
-        var lastActivityTime = new Date().getTime();
-        var lastSessionRefresh = new Date().getTime();
-        var activityRefreshInterval = 10 * 60 * 1000; // 10 minutes between auto-refreshes
-        var refreshDuration = 600; // Add 10 minutes (600 seconds) to session on each refresh
-        var minTimeBeforeRefresh = 5 * 60 * 1000; // Only refresh if at least 5 minutes passed since last refresh
-
-        // Track user activity (clicks, keypresses, mouse movements)
-        var activityEvents = 'click keypress mousemove touchstart';
-
-        $(document).on(activityEvents, function() {
-            var currentTime = new Date().getTime();
-            lastActivityTime = currentTime;
-
-            // Check if we should auto-refresh the session
-            // Conditions:
-            // 1. At least 5 minutes since last refresh
-            // 2. User has been active in the last 10 minutes
-            if ((currentTime - lastSessionRefresh > minTimeBeforeRefresh) &&
-                (currentTime - lastActivityTime < activityRefreshInterval)) {
-
-                // Check if session is still valid and has less than half its duration remaining
-                var sessionDuration = parseInt($('#temps_restant').val());
-                var now = Math.floor(new Date().getTime() / 1000);
-                var timeRemaining = sessionDuration - now;
-
-                // Auto-refresh if less than 30 minutes remaining
-                if (timeRemaining > 0 && timeRemaining < 30 * 60) {
-                    // Perform automatic session extension
-                    $.ajax({
-                        type: 'POST',
-                        url: 'sources/main.queries.php',
-                        data: {
-                            type: 'increase_session_time',
-                            type_category: 'action_user',
-                            duration: refreshDuration,
-                            key: store.get('teampassUser') ? store.get('teampassUser').key : ''
-                        },
-                        dataType: 'text',
-                        async: true,
-                        success: function(data) {
-                            try {
-                                var result = JSON.parse(data);
-                                if (result && result[0] && result[0].new_value !== 'expired') {
-                                    // Update temps_restant with new value
-                                    $('#temps_restant').val(result[0].new_value);
-                                    lastSessionRefresh = currentTime;
-
-                                    // Optional: Show subtle notification
-                                    console.log('Session automatically extended by ' + (refreshDuration / 60) + ' minutes due to user activity');
-                                }
-                            } catch (e) {
-                                // Silently fail
-                                console.log('Auto-refresh failed:', e);
-                            }
-                        },
-                        error: function() {
-                            // Silently fail
-                        }
-                    });
-                }
-            }
-        });
-
-        // Throttle the activity detection to avoid excessive checks
-        // Use a debounce-like mechanism
-        var throttleTimeout;
-        $(document).on(activityEvents, function() {
-            clearTimeout(throttleTimeout);
-            throttleTimeout = setTimeout(function() {
-                // Activity detected and throttled
-            }, 1000);
-        });
-    })();
-
     $(".show_hide_password a").on('click', function(event) {
         event.preventDefault();
         if($('.how_hide_password input').attr("type") === "text"){
