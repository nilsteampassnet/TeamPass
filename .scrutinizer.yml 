build:
  nodes:
    analysis:
      tests:
        override:
          - command: 'pecl install ldap || echo "LDAP installation failed, proceeding with build."'
          - command: 'composer install'
