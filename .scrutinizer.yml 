build:
  nodes:
    analysis:
      dependencies:
        override:
          - 'composer install'
          - 'if ! pecl list | grep -q ldap; then pecl install ldap || echo "LDAP installation failed"; fi'
